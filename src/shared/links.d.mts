// Hand-written declarations for links.mjs, for the same reason as plans.d.mts.

export const MAX_LINK_LENGTH: number;
export function normalizeLink(input: unknown): string | null;
export function linkHost(href: string): string;
