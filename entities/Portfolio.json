{
  "name": "Portfolio",
  "type": "object",
  "properties": {
    "domainId": { "type": "string" },
    "ownerUserId": { "type": "string", "description": "User who owns this portfolio" },
    "visibility": { "type": "string", "enum": ["DOMAIN_ONLY", "PRIVATE"], "default": "DOMAIN_ONLY" },
    "name": { "type": "string", "description": "Portfolio display name" }
  },
  "required": ["ownerUserId"]
}
